import { CollapsableListDirective } from './collapsable-list.directive';

describe('CollapsableListDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapsableListDirective();
    expect(directive).toBeTruthy();
  });
});
